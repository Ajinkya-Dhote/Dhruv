server {
    listen 80;
    server_name dhruv-ui;
    root /usr/share/nginx/html;
    index index.html index.html;

    location /api/ {
        proxy_pass http://dhruv-gateway:8081/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
