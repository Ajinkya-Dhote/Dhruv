server {
    listen 80;
    server_name dhruv-ui;
    root /usr/share/nginx/html;
    index index.html index.html;

    location /api/product/ {
        proxy_pass http://dhruv-gateway:8081/api/product/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
